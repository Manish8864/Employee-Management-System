# Frontend environment variables (Vite)

# Base URL that the frontend uses for API requests.
# In production you typically deploy backend separately and set:
# VITE_API_BASE_URL=https://YOUR_BACKEND_DOMAIN/api
#
# For local dev, you can leave it unset and run backend on http://localhost:5000
VITE_API_BASE_URL=

